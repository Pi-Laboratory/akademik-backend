{
  "host": "HOST",
  "port": "PORT",
  "postgres": "DATABASE_URL",
  "authentication": {
    "oauth": {
      "defaults": {
        "host": "HOST"
      },
      "redirect": "OAUTH_REDIRECT",
      "google": {
        "key": "OAUTH_KEY",
        "secret": "OAUTH_SECRET",
        "redirect_uri": "OAUTH_CALLBACK_URI"
      }
    }
  }
}
